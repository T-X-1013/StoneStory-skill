#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ $# -lt 1 ]]; then
  echo '用法：scripts/baoyu-chat <question>' >&2
  exit 1
fi

"${SCRIPT_DIR}/stonestory-chat" jia-baoyu "$*"
