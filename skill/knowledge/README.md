# Knowledge Assets

`skill/knowledge/` 用于存放《红楼梦》知识底座相关资产。

这一层后续适合放：

- 知识说明文档
- 检索索引说明
- 章节摘要资产
- 人物、关系、事件、引文等知识层定义
- 供问答或检索能力直接消费的 Markdown / JSON / JSONL 配置

当前该目录仍属于预留目录，后续随着知识底座建设继续补充。

维护原则：

- 优先存放可复用、可被程序消费的知识资产
- 尽量与 `data/output/` 中的结构化结果保持对应关系
